function analyzeData(array) { 
    console.log(array);

    while ((appSecurity="pr") && (appEmpInd="n")) {
        while (((appSelfCredit = "vg") || (appSelfCredit = "e") && ((appPartCredit = "vg") || (appPartCredit = "e")))) {
            if ((appEquity > ((appMortgage + appEquity) * .5)) || (appEquity > (appMortgage + appEquity) * .2)) {
                setIntRangeGroup = "A";
            } else { if ((appEquity > ((appMortgage + appEquity) * .35)) || (appEquity < (appMortgage + appEquity) * .21)) {
                setIntRangeGroup = "B";
            };
        };
      

} 